---
name: "CI Pipeline"

on: {}  # 无触发条件

jobs:
  build:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v4"
      - uses: "actions/setup-python@v4"
        with:
          python-version: "3.11"

# 文件末尾空行（必须保留）
